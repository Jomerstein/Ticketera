@model IEnumerable<ReservaTicket.Models.Entrada>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.espectaculo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.estaUsada)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.espectaculo.idEspectaculo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.estaUsada)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idEntrada">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idEntrada">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idEntrada">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
